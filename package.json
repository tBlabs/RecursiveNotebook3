{
  "name": "notes.api",
  "version": "1.0.0",
  "description": "",
  "main": "startup.js",
  "engines": {
    "node": "7.9.0",
    "npm": "4.2.0"
  },
  "scripts": {
    "clean": "rm server/bin -rf",
    "buildserver": "tsc -p server",
    "buildclient": "cd client && ng build",
    "buildall": "npm run buildserver && cd client && ng build",
    "start": "node server/bin/startup.js",
    "runclient": "cd client && ng serve",
    "runserver": "npm run start",
    "buildandrunserver": "npm run buildserver && npm run runserver",
    "deploy": "git push heroku master && heroku logs --tail",
    "fastdeploy": "npm run buildserver && git add . && git commit -m \"fast deploy\" && npm run deploy",
    "test": "npm run buildserver && jasmine --config=src/spec/support/jasmine.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jasmine": "^2.5.47",
    "@types/node": "^7.0.18",
    "typescript": "^2.3.2"
  },
  "dependencies": {
    "@types/body-parser": "^1.16.3",
    "@types/express": "^4.0.35",
    "@types/jasmine": "^2.5.41",
    "@types/jwt-simple": "^0.5.33",
    "@types/mongodb": "^2.2.2",
    "@types/uuid": "^2.0.29",
    "asyncawait": "^1.0.6",
    "body-parser": "^1.17.1",
    "express": "^4.15.2",
    "inversify": "^4.1.0",
    "jasmine": "^2.6.0",
    "jasmine-core": "^2.6.1",
    "jwt-simple": "^0.5.1",
    "mongodb": "^2.2.26",
    "os": "^0.1.1",
    "reflect-metadata": "^0.1.10",
    "uuid": "^3.0.1"
  }
}
